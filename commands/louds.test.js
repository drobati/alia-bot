const louds = require('./louds');

describe('commands/louds', () => {
    describe('should', () => {
        test('respond to delete', () => {
            expect(false).toBeTruthy();
        });
        test('respond to ban', () => {
            expect(false).toBeTruthy();
        });
        test('respond to unban', () => {
            expect(false).toBeTruthy();
        });
        test('delete loud with delete command', () => {
            expect(false).toBeTruthy();
        });
        test('add banned loud with ban command', () => {
            expect(false).toBeTruthy();
        });
        test('delete loud with ban command', () => {
            expect(false).toBeTruthy();
        });
        test('add loud with unban command', () => {
            expect(false).toBeTruthy();
        });
        test('delete banned loud with unban command', () => {
            expect(false).toBeTruthy();
        });
    });
});
